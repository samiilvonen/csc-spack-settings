# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs:
  - cgal
  - curl
  - gsl
  - jasper
  - jemalloc
  - openblas +consistent_fpcsr+locking+shared+pic threads=openmp
  view: false
  concretizer:
    unify: when_possible
  packages:
    all:
      compiler: [gcc@14.2.0]
      target: [zen2]
    boost:
      variants: +context+container+coroutine+fibber+icu~mpi cxxstd=17
    cgal:
      variants: +core
    gsl:
      variants: +external-cblas
    jasper:
      variants: +jpeg~opengl+shared build_type=Release
    openblas:
      variants: +consistent_fpcsr threads=openmp
