config:
  build_jobs: 20
  install_tree:
    projections:
      all: "{compiler.name}-{compiler.version}/{name}-{version}-{hash:6}"
    root: /appl/spack/v021/install-tree
  build_stage: /local_scratch/build/spack-stage
  source_cache: /appl/spack/v021/sources
